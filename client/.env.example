# Copy to .env for local overrides. Not required for local dev — the Vite
# proxy in vite.config.js forwards /api to http://localhost:4000 automatically.
# Set this in the Netlify build environment to point at the Railway API.
VITE_API_URL=http://localhost:4000
